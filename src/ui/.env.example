# =============================================================================
# RDF-StarBase UI Environment Configuration
# =============================================================================
# Copy this file to .env.local for local development or .env.production for builds

# Authentication Configuration
# -----------------------------------------------------------------------------
# Set to 'true' to require authentication for all UI access
# In development, this is typically false; in production, set to true
VITE_REQUIRE_AUTH=false

# Session timeout in milliseconds (default: 30 minutes)
# After this period of inactivity, users will be prompted to re-authenticate
VITE_SESSION_TIMEOUT=1800000

# API Configuration
# -----------------------------------------------------------------------------
# API base URL - leave empty for same-origin, or set for cross-origin API
# VITE_API_BASE_URL=https://api.example.com

# OAuth/OIDC Provider Configuration (for UI to know available providers)
# These should match what's configured on the API side
# VITE_OIDC_PROVIDERS=keycloak,azure_ad,okta

# Feature Flags
# -----------------------------------------------------------------------------
# Enable experimental features
# VITE_ENABLE_AI_GROUNDING=true
# VITE_ENABLE_SQL_EXPLORER=true
# VITE_ENABLE_STARCHART=true

# Telemetry (if enabled)
# -----------------------------------------------------------------------------
# VITE_TELEMETRY_ENDPOINT=https://telemetry.example.com/ingest
